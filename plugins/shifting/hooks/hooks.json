{
  "description": "Shifting plugin pipeline-artifact gatekeeper. Blocks reads of stale pipeline artifacts (existing-world.pl, hypothesis.pl, target-world.pl, model_results.pl, lean_proof_results.pl, adherence_facts.pl) by comparing mtimes against their upstream chain. Opt out per-session with SHIFTING_GATEKEEPER=off.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-chain.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
